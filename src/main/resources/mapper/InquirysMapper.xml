<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.admininterviewdotboot.mapper.InquiryMapper">
    <select id="getinquirylist" resultType="InquiryVO">
        SELECT * FROM Inquiry ORDER BY i_active = 0 DESC
    </select>
    <select id="getInquiryDetail"  resultType="InquiryVO" parameterType="String">
        SELECT *
        FROM Inquiry
        INNER JOIN User_t ON Inquiry.id = User_t.id
        WHERE Inquiry.i_idx = #{i_idx};
    </select>
    <update id="updatemail" parameterType="InquiryVO">
        UPDATE Inquiry
            SET i_active = 1
            WHERE i_idx = #{i_idx};
    </update>
</mapper>