<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.admininterviewdotboot.mapper.AdminMapper">
    <select id="getadminlist" resultType="AdminVO">
        SELECT * FROM Admin_t ORDER BY a_status = 1 DESC
    </select>
    <select id="getAdminDetail"  resultType="AdminVO" parameterType="String">
        SELECT *
        FROM Admin_t
        WHERE a_idx = #{a_idx};
    </select>
    <update id="editadmin" parameterType="AdminVO">
        UPDATE Admin_t
            SET a_id = #{a_id}, 
                a_name = #{a_name}, 
                a_phone = #{a_phone}, 
                a_email = #{a_email}
            WHERE a_idx = #{a_idx};
    </update>
    <update id="admindelete" parameterType="AdminVO">
        UPDATE Admin_t
            SET a_status = -1
            WHERE a_idx = #{a_idx};
    </update>
    <insert id="createUser" parameterType="AdminVO"> 
        INSERT INTO Admin_t (a_id, a_pwd, a_name, a_phone, a_email, a_status)
        VALUES (#{a_id}, #{a_pwd}, #{a_name}, #{a_phone}, #{a_email}, 0)
    </insert>
</mapper>